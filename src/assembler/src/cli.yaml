name: asm2bin
version: "1.0"
about: Compiler for asm->binary for nand2tetris project
author: Mark O. <mail.ormark@gmail.com>x

args:
  - INPUT:
      help: Sets the input file to use
      required: true
      index: 1
  - output:
      short: o
      long: output
      value_name: FILE
      help: Path to the file to write output
      takes_value: true
      required: true
      requires:
        - INPUT
